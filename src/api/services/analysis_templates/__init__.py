"""Analysis templates: named specs for a dashboard section.

A template is data (``models.AnalysisTemplate``). One generic builder
(``builder.apply_template``) runs every template. It pulls the data, makes
the widgets and writes one normal, editable section.
"""
