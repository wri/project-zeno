# Local development environment variables
# Copy this file to .env.local and modify as needed
# Only override the keys that differ from production .env

# Database connection (pointing to local Docker PostgreSQL)
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5433/zeno-data

# Test Database connection (pointing to local Docker PostgreSQL)
TEST_DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5434/zeno-data_test

# Langfuse connection (pointing to local Docker Langfuse)
LANGFUSE_HOST=http://localhost:3000
LANGFUSE_PUBLIC_KEY=lf_pk_1234567890
LANGFUSE_SECRET_KEY=lf_sk_1234567890

# API URLs for frontend
API_BASE_URL=http://localhost:8000

# OLLAMA
OLLAMA_BASE_URL=http://host.docker.internal:11434

COOKIE_SIGNER_SECRET_KEY="secret-cookie-key-123"
